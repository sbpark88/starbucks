// @ts-check

import { $, render } from "../../../../utils/render.js";
import template from "./ReserveStore.html";

const container = $(".animation-effect .reserve-store");

render(container)(template);
